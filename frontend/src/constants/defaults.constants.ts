export enum DefaultRoutes {
    Root = '/',
    RandomList = '/random-list',
    History = '/history',
    Profile = '/profile'
}

export enum DefaultURLs {
    RandomUserService = 'https://randomuser.me/api',
    BackendService = 'http://localhost:8810'
}